



Ansible

######################################

Ansible is allready pre-configured.

Go to /etc/ansible

make the .cfg file so that the ssh host_keys is set to false.

make your hosts in the host file

setup the vars for the hosts

check with a ping command if the hosts are online.

make a ansible-playbook with 4 router setup commands.

######################################

write a configuration manual for the Ansible server.


